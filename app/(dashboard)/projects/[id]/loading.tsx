import { ProjectDetailLoading } from '@/components/projects/project-detail-loading';

/**
 * Shows immediate feedback while a project detail route is loading.
 */
export default function ProjectDetailRouteLoading() {
  return <ProjectDetailLoading />;
}
